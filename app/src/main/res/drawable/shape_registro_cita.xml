<?xml version="1.0" encoding="utf-8"?>
<!-- shape_registro_cita.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo con esquinas redondeadas -->
    <corners android:radius="8dp" />

    <!-- Borde -->
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- Cambia el color según tu paleta de colores -->

    <!-- Color de fondo -->
    <solid android:color="@color/white" /> <!-- Cambia el color según tu paleta de colores -->
</shape>
